(function(){const box=document.getElementById('countdown');if(!box)return;const t=new Date(box.dataset.date);const d=document.getElementById('d');const h=document.getElementById('h');const m=document.getElementById('m');const s=document.getElementById('s');function tick(){const now=new Date();let diff=Math.max(0,t-now);const dd=Math.floor(diff/86400000);diff-=dd*86400000;const hh=Math.floor(diff/3600000);diff-=hh*3600000;const mm=Math.floor(diff/60000);diff-=mm*60000;const ss=Math.floor(diff/1000);d.textContent=dd;h.textContent=hh;m.textContent=mm;s.textContent=ss;}tick();setInterval(tick,1000);})();